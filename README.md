# pneumonia-detection-cnn-resnet50
Pneumonia Detection from Chest X-ray Images using CNN and ResNet50 Transfer Learning

The CNN baseline used grayscale X-ray images with input size 128×128×1. For ResNet50 transfer learning, images were loaded as RGB with input size 224×224×3 to match ImageNet pretrained model requirements.
